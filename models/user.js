class User {
  constructor(email, name, balance, isAdmin, picture) {
    this.email = email;
    this.name = name;
    this.balance = balance;
    this.isAdmin = isAdmin;
    this.picture = picture;
  }
}

export default User;
